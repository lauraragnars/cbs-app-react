import { StyleSheet } from "react-native";

export const inputs = StyleSheet.create({
  inputField: {
    borderColor: "black",
    padding: 10,
    borderStyle: "solid",
    borderWidth: 1,
  },
});
